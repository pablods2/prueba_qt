#include <iostream>  // Librería para manejar entradas y salidas

using namespace std;  // Evita tener que escribir "std::" antes de cada comando de la librería estándar

int main() {
    // Imprime un mensaje en la consola
    cout << "¡Hola, Mundo!" << endl;

    return 0;  // Devuelve 0 para indicar que el programa terminó correctamente
}
